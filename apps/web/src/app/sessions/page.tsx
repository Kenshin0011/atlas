/**
 * Sessions Management Page
 * セッション一覧・統計表示・エクスポート
 */

import { SessionsManager } from '@/features/components/SessionsManager';

export default function SessionsPage() {
  return <SessionsManager />;
}
