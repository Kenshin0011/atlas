/**
 * CTIDE Sessions Management Page
 * セッション一覧・統計表示・エクスポート
 */

import { SessionsManager } from '@/features/ctide-assistant/components/SessionsManager';

export default function SessionsPage() {
  return <SessionsManager />;
}
